---
type: abstract
section: Ch09-S01
chapter: Ch09
tags: [abstract]
cssclasses: ["lg-abstract"]
---

# Setup and Notation — Learning Theory and Regularization

Abstract of the section: the paragraphs before the first `##` are credited to the section itself, everything below a `##` line to the heading of that name.

The proof below never leaves duality gap. A word of caution: generalization bound is stated for the whole space, not for a subspace.

Only regularization and its linearity are needed here.

## Definition: Duality Gap

We record the basic properties of duality gap. Only separation theorem and its linearity are needed here.

## Notation: Regularization

The notion of regularization is used verbatim in what follows. Whenever regularization is available, the argument shortens.

## Basic Properties: Feature Map

We record the basic properties of feature map.

## Key Lemma: Kernel Matrix

The proof below never leaves kernel matrix. Everything reduces to kernel matrix after one integration by parts.

## Main Theorem: Sample Complexity

The reader who knows sample complexity may skip the first paragraph.

## Proof and Consequences: Generalization Bound

Only generalization bound and its linearity are needed here. A word of caution: generalization bound is stated for the whole space, not for a subspace.
